spring:
  application:
    name: OtelRezervasyon
  datasource:
    url: jdbc:postgresql://localhost:5432/OtelRezervasyon
    username: postgres
    password: test
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080